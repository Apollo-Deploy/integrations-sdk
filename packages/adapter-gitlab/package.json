{
  "name": "@apollo-deploy/adapter-gitlab",
  "version": "1.2.1",
  "description": "GitLab adapter for Apollo Deploy integration hub",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --build --watch",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist .turbo",
    "lint": "eslint src"
  },
  "dependencies": {
    "@apollo-deploy/integrations": "workspace:*",
    "@gitbeaker/rest": "^40.0.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.0.0"
  }
}
